export type Architect = {
  id: number;
  name: string;
  lastName: string;
  mail: string;
  permissions: string;
  celula: string;
  img?: string;
  position: string;
};

//puesto o cargo de trabajo es referido como position en el tipado.
